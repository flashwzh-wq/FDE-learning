# 代码库 + 速查表

> 按「查它的目的」分成两类：
> - **模板**：文字多、敲起来累的完整骨架，照抄后只换 `<尖括号>` 里的部分。
> - **速查表**：几个函数名，扫一眼就知道是什么、怎么用。
> 标准语法（如 `def` 函数定义）闭眼就会，不收录。
> 配合《错题本.md》用：错题本防坑，这里给骨架和速查。

---

## 一、模板（照抄型）

### 1. 命令行传参（argparse 四行）

```python
import argparse
parser = argparse.ArgumentParser()
parser.add_argument("<参数名>", help="<说明文字>")   # 想收几个参数就写几行
args = parser.parse_args()
变量 = args.<参数名>
```

- 变化点：`<参数名>`、`<说明文字>`
- 可选配件：`type=int`（自动转数字）、`default=0`（不给时的默认值）
- 带配件的完整写法（照抄改数字就行）：
  ```python
  parser.add_argument("--user", type=int, help="请填写作者编号", default=1)
  ```
- 用在哪：avg_argparse.py、query_store.py、report_store.py、exam_week7.py

---

### 2. 读文件 + 拆数据

把 txt 文件一行行读进来，按分隔符拆开。

```python
with open("<文件名>", "r") as f:
    for line in f:
        line = line.strip()          # 去掉行尾的换行符
        parts = line.split(",")      # 按逗号拆成一个列表
```

- 变化点：`<文件名>`、`","`（换成你的分隔符，比如 `";"`）
- 用在哪：report_store.py、price_report.py

---

### 3. 字典分组（一个键攒多个值）

把数据按某一列归类，同类装进同一个列表。

```python
分组 = {}
for line in f:
    parts = line.strip().split(",")
    键 = parts[0]
    值 = float(parts[2])
    if 键 not in 分组:      # 第一次见到这个键
        分组[键] = []        # 先挂一个空列表
    分组[键].append(值)      # 再往里装
```

- 变化点：`键`、`值`（换成你的列）；`float`（整数换 `int`，文字不转）
- 关键口诀：`=` 是「覆盖」，`.append()` 是「累加」
- 用在哪：report_store.py、price_report.py

---

### 4. 查不到别崩（错误处理）

取字典前先问一句"在不在"，不在就友好提示。

```python
if 键 in 字典:
    ...正常处理...
else:
    print("没查到")
```

- 用在哪：report_store.py、price_report.py

---

### 5. 联网请求 + 拆包（requests 三行）

去别人服务器上要数据，拆开成字典/列表。

```python
import requests
resp = requests.get("<网址>")     # ① 下单：把网址发出去
数据 = resp.json()                 # ② 拆包：JSON 变成字典（或列表）
```

- 变化点：`<网址>`
- 网址里藏"变化点"，用 f-string 拼（根治「字符串 + 数字忘 str()」的老坑）：
  ```python
  url = f"https://jsonplaceholder.typicode.com/posts?userId={args.user}"
  resp = requests.get(url)
  ```
- 关键口诀：`resp` 是「包裹」不是字典，**先 `.json()` 拆包，才能用 `[ ]` 取值**
- 用在哪：net_test.py、post_query.py、exam_week7.py

---

## 二、速查表（扫一眼）

### 三种容器（一眼分清）

| 类型 | 长相（符号） | 例子 | 取值 | len 问什么 |
|---|---|---|---|---|
| 字符串 str | `"..."` 引号 | `"hello"` | `s[0]` 第1字符 | 几个字符 |
| 列表 list | `[ ]` 方括号 | `[1,2,3]` | `lst[0]` 第1元素 | 几个元素 |
| 字典 dict | `{键:值}` 花括号 | `{"a":1}` | `d["a"]` 用钥匙 | 几对键值 |

**判断标志**：见 `[` `]` → 列表；见 `{` `:` `}` → 字典；见引号 → 字符串。

**最易混的两点**：
- 列表用【数字】当下标 `lst[0]`；字典用【字符串】当钥匙 `d["name"]`。报 `list indices must be integers` = 给列表塞了字符串钥匙。
- 容器能互相套：列表里套字典（JSON 常见）、字典里套列表，一层一层拆。

### 常用操作返回什么类型

| 操作 | 返回类型 | 备注 |
|---|---|---|
| `requests.get(url)` | Response 包裹 | 不是数据！先 `.json()` 拆包 |
| `resp.json()` | 列表 或 字典 | 看接口：/posts → 列表，/posts/1 → 字典 |
| `data[0]` | 列表第1个元素 | JSON 接口里通常是字典 |
| `sorted(东西)` | 永远是列表 | 喂啥都吐列表 |
| `d.keys()` | dict_keys | 套 `list()` 变列表 |
| `d.items()` | 键值对 | `for a,b in d.items()` |
| `input()` | 字符串 | 要数字先 `int()` |
| `len(东西)` | 整数 | 数个数 |
| `"a,b".split(",")` | 列表 | 字符串切列表 |

### 三个报错 = 类型搞混的报警

| 报错 | 意思 | 怎么治 |
|---|---|---|
| `list indices must be integers, not str` | 拿字符串当钥匙戳列表 | 列表用数字下标，或该用字典 |
| `KeyError: 'xxx'` | 字典里没这把钥匙 | `print(字典)` 看是不是空 `{}`，问题在上游 |
| `can only concatenate str` | 字符串 + 数字 | 用 f-string 或逗号拼接 |

### 类型转换

| 写法 | 作用 |
|---|---|
| `int("88")` | 字符串 → 整数 |
| `float("4.5")` | 字符串 → 小数 |
| `str(88)` | 数字 → 字符串 |

### 统计

| 写法 | 作用 |
|---|---|
| `sum(列表)` | 总和 |
| `max(列表)` | 最大 |
| `min(列表)` | 最小 |
| `len(列表)` | 个数 |
| `round(数, 1)` | 保留 1 位小数 |

### JSON 读写

```python
import json
json.dump(数据, f, ensure_ascii=False)   # 把数据写进文件
数据 = json.load(f)                        # 把数据从文件读出来
```
