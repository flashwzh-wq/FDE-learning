# 错题本

> 每日标准动作：学完后复盘当天 bug，归类记录。
> 格式：日期 | 文件 | 类别 | bug 描述 | 正确写法

---

## 2026-08-10（Day 10 — 字典、JSON）

### A. 字典-列表操作混淆（2 次）

| 文件 | bug | 正确 |
|---|---|---|
| day10_exercise.py | `score.append(x)` 往字典加东西 | `score[key] = value` |
| day10_final.py | `record["汇总"] = {"文本"}` 当成字典 | `{"key": value}` 必须冒号分隔 |

### B. 变量名拼写/不一致（3 次）

| 文件 | bug | 正确 |
|---|---|---|
| day10_exercise.py | 定义 `score = {}`，后面用 `scores.values()` | 统一用 `score` |
| day10_challenge.py | `json.dump(result, f)` 少了 s | `json.dump(results, f)` |
| day10_final.py | `hightest_name` / `avarage` | `highest` / `average` |

### C. 缩进/位置放错（3 次）

| 文件 | bug | 正确 |
|---|---|---|
| day10_exercise.py | `if "阿里" in score` 放在 for 循环里面 | 读完所有数据后再查 |
| day10_challenge.py | `import json` 放在循环里面 | 放文件最顶部 |
| day10_final.py | `record = {}` 放在循环里面（每轮重置） | 放 `for` 上面 |

### D. 遗漏括号/字符（3 次）

| 文件 | bug | 正确 |
|---|---|---|
| day10_practice.py | `part[1]` 缺 s | `parts[1]` |
| day10_challenge.py | `line.strip` 忘 `()` | `line.strip()` |
| day10_challenge.py | `"repor.json"` 缺 t | `"report.json"` |

### E. 类型混淆（5 次）

| 文件 | bug | 正确 |
|---|---|---|
| day10_exercise.py | 分数存成字符串 `"85"` | `int(parts[1])` |
| day10_challenge.py | 打印时 `line` 整行当输出 | 用 `parts[0]` `parts[1]` 分别取 |
| day10_final.py | `text = json.dumps(record)` 然后 `text.items()` | 字符串没有 `.items()`，直接遍历 `record.items()` |
| day10_final.py | `len(text)` 统 JSON 字符串长度 | `len(record)` 统字典条目数 |
| day10_final.py | 数字拼字符串没加 `str()` | `str(score)` |

### F. 用错对象/变量（5 次）

| 文件 | bug | 正确 |
|---|---|---|
| day10_retry.py | `record[parts[0]]` 拿到日期而非公司名 | 确认数据文件实际列顺序再定索引 |
| day10_retry.py | `json.dump(text, f)` 写了字符串不是字典 | `json.dump(record, f)` |
| day10_final.py | `record[parts[0]] = {parts[1]}` 集合语法 | `record[parts[0]] = int(parts[1])` |
| day10_final.py | 忘了写 `json.dump()` 全过程 | 对照题目要求逐条检查 |
| day10_final.py | `record["汇总"]` 用字符串代替字典 | `{"最高": name, "最高分": score}` |

---

## 2026-08-11（Day 11 — json.load、嵌套字典）

### 今日新增（4 次）

| 文件 | 类别 | bug | 正确 |
|---|---|---|---|
| review_0811.py | D | `record {"飞书":77}` 忘 `=` | `record = {...}` |
| review_0811.py | F | `items(record)` 当独立函数 | `record.items()` |
| review_0811.py | E | `v` 数字拼字符串忘 `str()` | `str(v)` |
| day11_exercise.py | F | `for k,v in f.items()` 遍历文件对象 | `data = json.load(f)` 先读，再 `data.items()` |
| day11_exercise.py | A | `record[k] = {v}` 集合语法（重现） | `record[k] = v` |
| day11_exercise.py | C | `if v < 60` 没排除"汇总"条目 | 合并到 `if k != "汇总"` 内 |

### 高频复发坑（跨天统计 ≥ 2 次）

| 类别 | 坑 | 累计 | 上次出现 |
|---|---|---|---|
| A | `{v}` 写成集合 | 3 次 | Day 10、11 |
| E | 数字拼字符串忘 `str()` | 3 次 | Day 10、11 |
| D | 忘 `=` / 忘 `()` / 忘 `s` | 4 次 | Day 10、11 |
| F | 遍历了文件对象而不是字典 | 2 次 | Day 10、11 |

### 新增概念混淆
- `load` / `dump` 方向记反：load = 装回来（→ dict），dump = 倒出去（→ file/str）

---

## 2026-08-13（Day 13 — Git 推送 + 两周综合复习）

### 今日新增（6 次）

| 文件 | 类别 | bug | 正确 |
|---|---|---|---|
| review_dict_list.py | A | `score.append = (88,79,92)` 把 append 当赋值 | `append(88)` 逐个调用，方法要加括号 |
| review_dict_list.py | B | `score` / `scores` 单复数混用 | 统一一个名字 |
| review_weekly.py | D | `def word(scores_group)` 忘冒号 | `def word(scores_group):` |
| review_weekly.py | D | `.items` 忘括号 | `.items()` |
| review_weekly.py | E | `int(v)` 把 round 过的小数砍成整数（88.3→88） | 平均分已是小数，直接用 `v`，别再 int |
| review_weekly.py | F | 函数名 `word` 被 `word = "优秀"` 覆盖 | 函数名也是变量，别当普通变量名用 |

### 高频复发坑更新

| 类别 | 坑 | 累计 | 上次出现 |
|---|---|---|---|
| D | 方法名/函数忘 `()` 或忘冒号 `:` | 6 次 | Day 13（`.items`、`def` 冒号） |
| B | 单复数变量名混用（score/scores） | 4 次 | Day 13 |

### 新增概念提醒
- **平均分是小数，别再 `int` 砍它**——`round` 之后就是小数，`int()` 会硬砍掉小数点（不是四舍五入）
- **函数名也是变量**——别拿函数名当普通变量名赋值，会被覆盖

---

## 类型速查表

| 代号 | 类别 | 自查口诀 |
|---|---|---|
| A | 字典-列表混淆 | 字典用 `[] =`，列表用 `.append()` |
| B | 拼写/不一致 | 跑之前 `cat` 看变量名一致性 |
| C | 位置放错 | "每行做"放循环里，"读完做"放循环外 |
| D | 遗漏字符 | `()` 跟函数，`"` 成对，`.strip` 带括号 |
| E | 类型混淆 | 字符串 ≠ 数字，`dumps` 后就不是字典了 |
| F | 用错对象 | dump 写字典不是字符串；索引用前确认列顺序 |

---

## 2026-08-15（Day 15 — argparse + 综合复习）

### 今日新增（2 次）

| 文件 | 类别 | bug | 正确 |
|---|---|---|---|
| avg_argparse.py | D | 漏写 `args = parser.parse_args()`，第③步漏了 | 四步骨架一步不能少 |
| query_store.py | F | print 标签照抄旧代码（"查询品牌"却打印城市） | 引号里的文案必须跟数据对上 |

### 新盲区（已补）
- **sum / max / min / len 没学过**——之前找极值一直用循环硬绕，今天补上（喂列表、吐数字）。
- 三个复习题因此全部卡住——根子是"前提函数不认识"，不是逻辑不会。

### 亮点
- report_store.py 独立把 `append` 提到 if 外面只写一次，结构理解到位

---

## 2026-08-17（Day 17 — glob 文件批处理）

### 今日新增（4 次）

| 文件 | 类别 | bug | 正确 |
|---|---|---|---|
| batch_report.py | D | `glob.glob("data/*.text")` 拼成 `.text`（应为 `.txt`）→ files 空列表，程序空转无输出 | `"data/*.txt"` |
| batch_report.py | E | `int(parts[2])` 把评分小数 4.5 砍成 4 | `float(parts[2])` |
| batch_report.py | D | `for k,v in score_group:` 忘 `.items()` | `for k,v in score_group.items():` |
| batch_report.py | E | `"...平均分：" + float(average)` 字符串 + 数字 | `+ str(average)` |

### 高频复发坑更新（都 ≥ 3 次了，重点盯）

| 类别 | 坑 | 累计 | 上次出现 |
|---|---|---|---|
| E | 数字拼字符串忘 `str()` | 4 次 | Day 10、11、13、17 |
| D | 忘 `.items()` / 括号 / 冒号 | 7 次 | Day 17 |
| E | `int` 砍小数（平均分/评分本应是小数） | 3 次 | Day 13、17 |

### 新概念提醒
- **"没报错但没输出"的诊断思路**：先怀疑第一步就扑空了（如 glob 通配符拼错 → 空列表 → for 不执行）。退出码 0 + 无输出 ≠ 没 bug，可能是循环根本没进。
- **glob 通配符拼写**：`*.txt` 的 `txt` 少打一个字母，程序不报错、静默交白卷——比报错更隐蔽，跑之前先 `print(files)` 看一眼扫到几个文件。

### 强记忆锚点（泽华自己复盘出的）
- **`.items()` 口诀**：`for` 左边写**一个**变量 → 裸遍历（只拿键）；写**两个**变量 `k, v` → 必须 `.items()`（拿键+值）。泽华原话「我记得要返回两个值，但忘了 items」——把"要几个值"直接绑在"左边几个变量"上，扫一眼就知道加不加。

---

## 2026-08-18（Day 18 — requests 联网 + f-string）

### 今日新增（6 次）

| 文件 | 类别 | bug | 正确 |
|---|---|---|---|
| post_query.py | F | `f".../{args}"` 把整个 Namespace 盒子塞进 URL | `f".../{args.id}"` 取盒子里的那一格 |
| post_query.py | F | `resp["title"]` 把 Response 对象当字典 | 先 `data = resp.json()` 拆包，再 `data["title"]` |
| post_query.py | B | `date = resp.json()` 拼成 date（日期） | `data`（数据），date/data 是两个词 |
| exam_week7.py | F | `for a,b in v.items()` 遍历错对象（v 是单篇帖子字典，不是装列表的 title_group）→ `sum(b)` 报 "int not iterable" | 遍历 `title_group.items()`，b 才是标题长度列表 |
| exam_week7.py | D | `arg.user` 少一个 s | `args.user` |
| exam_week7.py | E | `"..." + arg.user` 字符串 + 整数 | 逗号 `print("...", args.user, "...")` |

### 新概念提醒
- **f-string**：字符串前加 `f`，变量用 `{}` 包起来，自动转字符串——根治"字符串+数字忘 `str()`"（E 类累计 4 次）
- **requests 拆包**：`resp` 是 Response 对象（"包裹"），不是字典，**不能 `[]` 取值**；必须 `resp.json()` 变字典再取。跟 W4 的 `json.load` 思路一致，只是来源从"硬盘"换成"网络"。
- **args 是盒子不是值**：`args` 是 Namespace 对象，要取某个参数得用 `args.参数名`（如 `args.id`），不能直接 `{args}`。
- **联网返回两种形状**：`/posts/1` 返回**一个字典**（单条）；`/posts?userId=1` 返回**一个列表**（多条）。拆包后先想清楚是字典还是列表，再决定 `len()` 数数量、还是 `for` 遍历取每条的值。

### 下午续（exam_todo 待办完成率 + review_posts 综合复习）

| 文件 | 类别 | bug | 正确 |
|---|---|---|---|
| exam_todo.py | D | `fhttps://...` f-string 忘引号 | `f"https://..."` |
| exam_todo.py | E | `round(done/total, 3) * 100` 先 round 再乘 → 55.00000000000001 | `round(done/total * 100, 1)`，先乘后 round |
| review_posts.py | F | `max_name = name`（name 是 userId）→ 打出 "5" 不是标题 | 要记录 `v["title"]`，别记 userId |
| review_posts.py | F | 改 `name = v["title"]` 修极值，却把①分组连带改坏 | 一个变量被两处共用，改一处牵动另一处 |

### 新概念（下午新增）
- **浮点数精度**：`0.55` 二进制存不精确，一乘 100 尾巴露出来。口诀「**round 永远放最后，先算完再修小数位数**」
- **跑通 ≠ 正确**：程序没报错但答案不对（打出 "5" 不是标题），比报错更隐蔽。跑完要对照题目，把每条输出读一遍
- **变量复用耦合**：`name` 被①②两处共用，①要 userId、②要 title，一个变量装不下两个需求。解法：各取各的字段，或拆成两个变量（uid / title）
- **验货三步**：调接口取货前，先 `print(type(data))` / `print(len(data))` / `print(data[0])` 摸清返回结构，别靠猜

### 晚间续（top3 止损 + data_check 体检单）

| 文件 | 类别 | bug | 正确 |
|---|---|---|---|
| top3_report.py | B | 函数名 `title_line` 里面却 `len(v["body"])`，名字和内容打架 | 函数名 = 说明书，取正文就叫 `body_line`，把 `title_line` 让给② |
| top3_report.py | F | 排序按 title 排了，打印却取 `len(body)`，前后字段不一致 | 「排序按什么排，打印就按什么打」一条线贯穿 |
| data_check.py | B | `print(平均值, float(argparse))` 把 `average` 打成 `argparse` | `print("平均值：", average)`，average 已 round 过不必再 float |

### 新认知（重点）：IDE 自动联想「前缀撞车」是 D/B 类笔误的总根源
- 泽华自己发现：敲 `a` → 联想弹出 `average / argparse / args / add_argument`，手快回车就塞错词
- 回看历史笔误，几乎全是**前缀撞车**：`argparse`/`average`（a 开头）、`date`/`data`（da）、`args`/`arg`（arg）
- 防身口诀：**按回车前，眼睛扫一眼光标后是不是你要的那个词，尤其 a/da/arg 开头**；发现不对按 Esc 关掉弹窗自己敲全
- 底层习惯跟「验货三步」一致：**不信任"看起来对"，要验证"真的是它"**

---

## 2026-08-19（Day 19 — POST 送货 + 告警工具闭环）

### 今日新增（7 次）

| 文件 | 类别 | bug | 正确 |
|---|---|---|---|
| warmup.py | D | URL 末尾多空格 `posts/3 ` → 404 空字典 → KeyError | URL 干净结尾 `posts/3"` |
| post_create.py | D | URL 末尾空格（warmup 同款，第二次） | 同上 |
| post_create.py | D | 键名 `"title:"` 多冒号 → 钥匙对不上 | `"title"`，冒号不是钥匙的一部分 |
| alert_push.py | D | `request.get` 少 s | `requests.get`（import 的是复数） |
| alert_push.py | F | `len(data["title"])` 拿列表当字典，循环里该用 v | `len(v["title"])` |
| alert_push.py | D | `parse.add_argument` 少 r | `parser.add_argument`，建的和用的名字一模一样 |
| exam_todo.py | B | `print(..., average)` 惯性词，变量其实叫 `rate` | `rate`——变量名跟着装的东西走 |

### 高频复发坑更新

| 类别 | 坑 | 累计 | 上次出现 |
|---|---|---|---|
| D | 字符毛刺：URL 空格 / 键名冒号 / 漏字母（s、r） | 今天 5 次 | Day 19 |
| B | 惯性词/张冠李戴（average 顶替 rate，早前 argparse 顶替 average） | ≥ 4 次 | Day 18、19 |

### 新概念
- **POST vs GET**：`get` 取货、`post` 送货（多一个 `json=` 参数装你要发的字典）。套路永远 `requests.xxx` + `resp.json()`，只变"发什么动作、回什么反馈"。
- **`resp.json()` 拆的是「服务器回执」，不是「你发出去的货」**——jsonplaceholder 会原样返回你发的内容造成假象，真实 API（如企微）只回 `{"errcode":0}`，根本不含原文。
- **status_code**：`200` 取货成功、`201` 创建成功、`404` 没找到、`500` 服务器崩。发完先看它，`200`/`201` 才算成。

### 下午续（comment_alert 巩固题 — 评论异常监控）

| 文件 | 类别 | bug | 正确 |
|---|---|---|---|
| comment_alert.py | F | `data_out = resp.json()` 拆错变量——POST 的回执装在 `resp_out` 里，却从 `resp`（GET 的）拆，拆成 500 条列表 | `data_out = resp_out.json()`，拆哪个响应就用哪个 resp |
| comment_alert.py | F | `data_out.status_code` —— status_code 是「包裹」（Response）的属性，不是拆包后字典的属性 | `resp_out.status_code`（status_code 在 resp 上，id 在 data 上） |
| comment_alert.py | B | `"报错回执："` 一字之差，应是 `"告警回执："` | 文案跟用途对上 |

### 新认知（重点）：变量「张冠李戴」的根治 = 起名自带方向
- 泽华烦躁「为什么老犯低级错误」→ 归因：**不是粗心，是长得像的变量名太多（resp/resp_out/data/data_out 挤在一起），工作记忆盯不住**
- 根治：**起名把差异写到明面上，别考验眼睛**。`resp_get` / `resp_post`（差在词根 get/post），不要 `resp` / `resp_out`（差在容易漏看的后缀）
- **「包裹 vs 货」分层要焊死**：`status_code` 是快递单信息，长在 `resp`（包裹）上；`id`/`title` 是货里的东西，要 `resp.json()` 拆包后用钥匙取。两个东西住两个地方。

### 教练失误教训（泽华值得记，比语法值钱）
- 我给 `--limit 80` 的验证靶子算错了（把 body 长度和 title 长度搞混），害泽华用正确代码对了个错靶子、怀疑自己。
- 反向印证一句话：**输出和预期不符时，先别急着怀疑代码，也验一下「预期本身对不对」**——「跑通 ≠ 正确」反过来也成立：预期错了，正确输出也会显得"不对"。

---

## 2026-08-20（Day 20 — 作品包装 + 字典计数器/累加）

### 今日新增（2 个关键坑）

| 文件 | 类别 | bug | 正确 |
|---|---|---|---|
| author_workload.py | F | `total["uid"]` 把字典键写成字符串 "uid" → 10 个作者塌成 1 个键，值反复覆盖只剩最后一篇 146 字 | `total[uid]`，键用**变量** uid（每轮循环变 1~10） |
| （切片口头测验） | 切片 | `nums[:3]`、`nums[1:4]` 都「多取一个元素」，把结束位置当**闭区间**（含） | 左闭右开，`nums[a:b]` 共取 **b-a** 个 |

### 新概念（重点）
- **变量 vs 字符串**：`uid` 是变量（每轮变），`"uid"` 是字符串（永远不变的三个字母）。键写错成字符串**不报错、能跑通、输出还像模像样**，但结果整个错——比报错更隐蔽。自查：**要"动态"就用变量（无引号），要"固定"才用字符串（有引号）**。
- **切片左闭右开**：`nums[a:b]` 取 b-a 个元素，右边终点「不含」。口诀「个数 = 右减左」。

### 高频复发坑更新

| 类别 | 坑 | 累计 | 说明 |
|---|---|---|---|
| F | 变量/字符串搞混（`uid` vs `"uid"`） | 新家族 | 与 resp/resp_out、date/data 同根——「长得像但不是一个东西」 |
| D | 字符毛刺（ttile/title、request/requests 等） | 跨天 ≥ 10 次 | 持续头号坑 |

---

## 2026-08-21（Day 21 — W8 数据库 SQLite · CRUD 全通关）

### 今日新增（5 个关键坑）

| 文件 | 类别 | bug | 正确 |
|---|---|---|---|
| visits_db.py | D 字符毛刺 | `INTERGER` 少个 E | `INTEGER`。**类型名拼错 SQLite 宽容，不报错**（阴） |
| visits_db.py | D 字符毛刺 | `VAKUES`（L 打成 K） | `VALUES`。**语法关键字拼错会报 syntax error**（好，帮你抓出来） |
| visits_db.py | 字符串 vs 变量 | `WHERE date = date.today()` 把函数写进双引号字符串里 | 先 `today = str(date.today())` 在 Python 里算好，再用 `?` 填 |
| visits_db.py | 元组打包 | `execute("SQL", "A", today)` 多值散开 → 3 个参数报错 | `execute("SQL", ("A", today))` 值要**包成一个元组** |
| visits_db.py | D 字符毛刺 | `conn,commit()` 逗号 | `conn.commit()` 点号 |

### 新概念（重点）
- **元组 = 定死的容器**：四大容器——字符串 `""`（不可改）、列表 `[]`（可改）、元组 `()`（不可改）、字典 `{}`（可改）。口诀：**会变用列表/字典，定死用元组**。
- **括号 = 纸箱（打包）**：函数调用里逗号是「分隔参数」，括号把逗号「圈」进去变成「元组内分隔」。`execute` 第二个参数永远是「一个包」，单值也要 `(today,)` 加逗号。
- **`WHERE` vs `if`**：`if` 是 Python 的（分岔路口，控制流程），`WHERE` 是 SQL 的（筛子，挑哪些行），住在不同世界，各管各的。
- **先删后插**：`DELETE WHERE date=?` 放最前 + `INSERT` 在后，每跑一次都干净留 1 条，不再越攒越多。
- **`IF NOT EXISTS` 有惰性**：表建出来后，改建表代码再跑不会重建，要删库重来。
- **DELETE 加 WHERE 才精准**：`DELETE FROM visits` 无 WHERE 会清空整张表，最危险的一刀。

### 高频复发坑更新
| 类别 | 坑 | 累计 | 说明 |
|---|---|---|---|
| D | 字符毛刺（INTERGER/VAKUES/ttile/request…） | 跨天 ≥ 15 次 | **今天一天踩 3 次**（INTERGER、VAKUES、conn,commit），仍是头号坑，敲高频词后务必逐字母扫一眼 |
| F | 字符串 vs 变量（date.today()、uid） | 反复 | 「写进双引号里的东西 Python 不会算」这一条今天又踩（date.today()），需重点盯 |

