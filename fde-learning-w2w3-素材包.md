# 素材包：我用 WorkBuddy 学写代码 · 最近两周（第 3-4 周）

> 本文是素材，不是文章。整理自吴泽华（32 岁、零编程基础、前腾讯企业微信品牌经理）在 2026 年 8 月用 WorkBuddy 学习 Python 的原始记录。
> 起止日期：**2026-08-08（周六）～ 2026-08-21（周五）**，共 14 天（其中 8/16 无学习记录，实际学习日约 13 天）。
> 前一篇（第一周）已写"7 天跑通第一个程序，围绕四个功能点展开"。**本篇素材只覆盖这两周的新东西，不与前一篇重复。**

---

## ① 时间线与进度

### 这两周学了什么（按主题，具体到技术点）

| 日期 | 模块 | 具体内容 |
|---|---|---|
| 8/8 | W3 收尾·文件读写 | `open("r"/"w")`、`f.read()`/`f.write()`、`return (a, b)` 一次返回多个值、作用域 |
| 8/9 | W4·列表 | 方括号造列表、`[索引]` 取值、`.append()`、`for x in 列表` |
| 8/10 | W4·字典+JSON | `{}` 造字典、`.items()`、`json.dumps`/`json.dump`、`import`、`type()` |
| 8/11 | W4·嵌套字典 | `json.load`/`json.loads`、`data["汇总"]["最高"]` 层层打开 |
| 8/12 | W5·Git 基础 | `git init`/`add`/`commit`/`log`（当日日志缺失，有 `review_git.py` 留痕） |
| 8/13 | W5·Git 推远程 | `git remote add origin`、`git branch -M main`、`git push`、PAT token |
| 8/14 | W5·收尾 | README、`git push` 日常工作流、周五小考 |
| 8/15 | W6·CLI 工具 | `sys.argv` 命令行传参、`argparse` 四步骨架、`sum`/`max`/`min`/`len`、KeyError 错误处理 |
| 8/17 | W6·批量处理 | `glob` 扫整个文件夹、文件批处理 |
| 8/18 | W7·联网 API | `requests.get()`、`status_code`、`resp.json()` 拆包、f-string、虚拟环境 venv |
| 8/19 | W7·POST+告警 | GET/POST 对比、告警工具闭环、`--limit` 参数、数据类型速查表 |
| 8/20 | W7·收口 | 作品三件套（requirements.txt + README）、字典计数器、字典累加、`not in`、切片复习 |
| 8/21 | W8·数据库 SQLite | 建库建表、INSERT、SELECT、UPDATE、DELETE（CRUD 全通关）、`date.today()`、元组 |

**一句话概括主线**：这两周从"能读写文件"一路推到"程序第一次联网抓数据"再到"数据能存进数据库、以后随时查"——把 Python 从"跑一下吐几行字"变成"会留下东西在地上"。

### 可量化的数据（均为真实统计）

- **14 天**，覆盖 6 个模块（W3 收尾 → W4 列表/字典/JSON → W5 Git → W6 CLI → W7 联网 → W8 数据库）
- **新建/修改 .py 文件 55 个**（8/8 之后），**约 1067 行代码**（排除 .venv 虚拟环境）
- **25 次 git commit** 推上 GitHub 公开仓库（`flashwzh-wq/FDE-learning`）
- 学习节奏：原计划每天早上 1 小时，但这两周出现大量**晚间加练**——8/11 晚上 21:48、8/13 深夜、8/18 晚间、8/19 晚间、8/20 一天 6 个节点（早晨热身 + 上午作品 + 中午 15 分钟练习 + 下午小考 + 晚上两道加练）
- 每天跑通的程序：每个学习日至少 1-2 个，两周累计跑通的完整程序约 30+ 个
- 期间建立了两个长期资产：`错题本.md`（A-F 六类坑分类，累计 19KB）、`代码模板库.md`（模板 + 速查表两类）

---

## ② 名场面（5 个）

### 场面 1：跑通了，但结果整个是错的（8/20 晚间）

泽华写 `author_workload.py`，统计"每个作者一共写了多少字"。代码跑通了，输出却是一条诡异的结果：字典里本该有 10 个作者（编号 1~10），现在只剩一个 `{'uid': 146}`，冠军行打印"作者 uid 写了 146 字，最多"。

他以为写对了，说"你帮我检查"。真正的病根在 `total[uid]` 和 `total["uid"]` 之间——**多打了一对引号**。`uid` 是变量（每轮循环变 1、2、3…10），`"uid"` 是永远不变的三个字母。加了引号，10 个作者全部挤进同一个格子里互相覆盖，最后只剩"最后一篇文章 = 146 字"。

那一刻的心理活动（从对话还原）：他"跑完没急着交"，而是先觉得"输出看着不对劲"，于是停下来问"你帮我检查"。这个"觉得不对劲"的直觉，比代码跑通本身更值钱——因为这种 bug 不报错、能跑通、输出还像模像样，只有"读一遍输出觉得不对"的人才能抓住。

### 场面 2：数据库里跑出"两条一模一样的星巴克"（8/21）

泽华第一天学 SQLite，写 `visits_db.py` 往数据库里塞一条"星巴克"拜访记录。跑一次，屏幕打印 `[(1, '星巴克', '2026-08-21', '聊了会员体系')]`。再跑一次，变成了两条——id 一个 1、一个 2，内容一模一样。

他有点懵，以为出 bug 了。实际这是数据库的"累积"特性：**写进去的东西，不会因为程序重跑就消失，只会越攒越多**。这跟他之前学的完全相反——以前 GET 抓数据是"用完就丢，下次重新抓"，现在第一次体会到"写一次，永远在"。

这个瞬间是他编程认知的一个分水岭：他发现自己写了一个"会留东西在地上"的程序——磁盘上真的多了一个 `visits.db` 文件，12288 字节，跟那些 .py 文件并排躺在那。

### 场面 3：README 一句话，"写回数据"还是"推送告警"（8/20 上午）

泽华第一次给代码写 README（作品三件套：代码 + requirements.txt + README）。要求他用一句话说清 `alert_push.py` 干了什么。

他的初稿是 4 句，其中一句写"把这个问题记录**重新写回原网址里**"。这一句被点破了：他实际干的不是"改别人网站的数据"，而是"**发一条告警消息过去**"——`jsonplaceholder` 是个假数据站，你 POST 一条消息，它假装收下、回你一个 id，这个动作的真实含义是"把异常情况推送出去，让该知道的人知道"。

最后压成一句话："抓取文章列表，找出标题超过 `--limit` 字的异常文章，统计数量并自动推送一条告警。" 他认可了。这是他第一次被迫用"陌生人视角"审视自己的代码——以前代码是写给自己的，这次是写给一个完全没看过他代码的人看的。

### 场面 4："为什么老犯低级错误，烦躁"（8/19）

泽华写 `comment_alert.py`（评论异常监控），连续几次把 `data_out = resp.json()` 写成了 GET 的 `resp`（正确应该是 `resp_out`）。报错停在 `data_out["id"]`，报的是 `TypeError: list indices must be integers`，可真正写错的在上一行。

他情绪上来了，原话："为什么又错了这么低级的问题，烦躁。"

归因最后落在了"不是粗心"上：长得像的变量名太多了（`resp` / `resp_out` / `data` / `data_out` / `date`），工作记忆盯不住。根治办法是**起名自带方向**——以后直接叫 `resp_get` / `resp_post`，词根 get/post 一眼就分得清谁是谁。这个"起名自带方向"的心法，成了他之后一直沿用的习惯。

### 场面 5：教练算错了靶子，他反过来怀疑自己（8/19）

泽华给 `alert_push.py` 加了 `--limit` 参数，跑到 `--limit 80` 时，输出跟教练给的"预期答案"对不上。他第一反应是"我刚刚复查了我的代码，没有写错啊"。

结果查出来，**是教练（AI）把验证靶子算错了**——把 body 长度和 title 长度搞混了，说"应 3 篇"，实际标题最长才 79 字、超过 80 是 0 篇。他用正确代码对着错误靶子，白白怀疑了自己一轮。

这条教训被记进了错题本，而且成了后面反复用的一条原则："**输出和预期不符时，先别急着怀疑代码，也验一下'预期本身对不对'**"。这是他第一次在跟 AI 协作时，明确意识到"AI 也会错，而且是它错、不是我的代码错"。

---

## ③ 踩坑清单

> 每条按"怎么踩的 → AI 怎么定位的 → 最后怎么解决"整理。以下坑全部来自真实报错和对话记录。

### 坑 1：`total[uid]` 写成 `total["uid"]`（变量 vs 字符串）
- **怎么踩的**：写字典累加时，给键 `uid` 多加了一对引号。
- **AI 怎么定位的**：先跑出来看到字典塌成 `{'uid': 146}`，再追问"为什么加了引号就全错"，用"变量每轮变、字符串永远不变"讲清。
- **怎么解决**：去掉引号，`total[uid] = total[uid] + len(v["body"])`。记一条口诀——要"动态"用变量（无引号），要"固定"才用字符串（有引号）。

### 坑 2：`INTERGER` 少个 E（类型名拼错不报错）
- **怎么踩的**：建表写 `id INTERGER PRIMARY KEY`，把 INTEGER 拼错。
- **AI 怎么定位的**：跑通了、文件生成了、表也建了，但 AI 主动查库发现拼错的 `INTERGER` 静静躺在库里。
- **怎么解决**：改代码 + 删掉刚生成的 `visits.db` 重跑。关键认知：**类型名拼错 SQLite 宽容（看到 INT 开头就当整数），不报错，比报错更阴**。

### 坑 3：`VAKUES`（L 打成 K，语法关键字拼错报错）
- **怎么踩的**：写 INSERT 把 VALUES 拼成 VAKUES。
- **AI 怎么定位的**：`sqlite3.OperationalError: near "VAKUES": syntax error`，报错直接指向拼错处。
- **怎么解决**：改成 VALUES。关键认知：**语法关键字拼错会报错（反而好，帮你抓出来），类型名拼错不报错（才阴）**——两类拼错待遇不同。

### 坑 4：`data_out = resp.json()` 拆错变量（resp vs resp_out）
- **怎么踩的**：POST 之后应该拆 `resp_out`（回执），却写了 GET 的 `resp`。
- **AI 怎么定位的**：报错行是 15 行的 `data_out["id"]`，但真正写错在 14 行。用"报错行 ≠ 真正错行"点破——`resp.json()` 拆的是 10 个用户（列表），拿 `"id"` 去索引列表就报 `list indices must be integers`。
- **怎么解决**：改成 `resp_out.json()`。归入"惯性词张冠李戴"家族，根治办法=起名自带方向（`resp_get` / `resp_post`）。

### 坑 5：切片"多取了一个元素"（左闭右开）
- **怎么踩的**：口头测验 `nums = [10,20,30,40,50,60]`，把 `[:3]` 答成 4 个元素、`[1:4]` 答成 4 个，都把结束位置当成了"闭区间"。
- **AI 怎么定位的**：指出前两题都"多取了一个"，用减法验算——`nums[a:b]` 一共取 b-a 个。
- **怎么解决**：口诀"左闭右开，个数 = 右减左"。这条其实是两周前说"`[:3]` 我不记得了"的老知识点，隔了两周回来测，骨架还在、细则生锈。

### 坑 6：`date.today()` 写进双引号字符串里
- **怎么踩的**：写 `DELETE ... WHERE date = date.today()`，把函数调用塞进 SQL 字符串。
- **AI 怎么定位的**：`near "(": syntax error`——字符串里的内容 Python 不会去算，把 `date.today()` 原样丢给了 SQLite。
- **怎么解决**：先在 Python 里 `today = str(date.today())` 算好，再用 `?` 占位符填进去。

### 坑 7：双 Python 冲突 + 虚拟环境（8/18 的头号环境坑）
- **怎么踩的**：电脑上有两个 python3（终端 Homebrew 3.14.6、WorkBuddy 环境 3.13.12），requests 装错了位置，终端跑报 `ModuleNotFoundError`。
- **AI 怎么定位的**：排查出两个 Python 版本，Homebrew 那个受 PEP 668 保护不让直接 pip install。
- **怎么解决**：建 `.venv` 虚拟环境装 requests，从此每次开工先 `source .venv/bin/activate`。这个习惯对后面所有联网程序都适用。

### 坑 8：中文引号（连踩 3 次）
- **怎么踩的**：`git commit -m "..."` 用了中文 `""`，出现 `dquote>` 卡住；还误把 vim 编辑器打开（`git commit` 忘 `-m`）。
- **AI 怎么定位的**：识别出是中文引号，让 Ctrl+C 取消重输。
- **怎么解决**：命令行引号必须英文直引号 `"`。累计 3 次（跨两天）。

### 坑 9：round 放最后（浮点精度）
- **怎么踩的**：写 `round(done/total, 3)*100`，输出 `55.00000000000001 %`。
- **AI 怎么定位的**：浮点精度问题，`round` 应该包住整个乘法。
- **怎么解决**：`round(done/total*100, 1)`，口诀"round 放最后"。

### 坑 10：字符毛刺家族（头号坑，跨天 ≥15 次）
- **成员**：`ttile`→`title`、`request`→`requests`、`parse`→`parser`、`date`→`data`、URL 末尾多空格、键名 `title:` 多冒号、`conn,commit`（逗号应是点号）、`len[v]`→`name[v]`……
- **AI 怎么定位的**：多数是"照抄没逐字母核对"，后期归因发现一个总根源——**IDE 自动联想的前缀撞车**（argparse/average、date/data、args/arg），敲到一半 tab 补全就补错了。
- **怎么解决**：养成"敲完高频词回扫一眼字母再往下写"的习惯；每日"错题回顾"环节专盯这个家族。

---

## ④ 真实感受（第一人称，口语化）

1. **"为什么又错了这么低级的问题，烦躁。"**（8/19）——连写 `resp` / `resp_out` 拿错两次之后。后来想通了：不是粗心，是长得像的变量名太多，脑子里的工作记忆盯不住。从那以后给变量起名都带方向（`resp_get` / `resp_post`）。

2. **"我感觉我很难背下来写法，只能去已经写过的代码里借鉴。"**（8/18）——学了两周多，第一次诚实地承认：记不住语法，得靠翻旧代码。AI 没有逼他背，反而帮他划了条线：脑子只装 5 条口诀，完整写法全丢进模板库和错题本，要用了去查。这条"该背 vs 该查"的分工，让他松了一大口气。

3. **"老坑反复踩，缺乏一遍过的爽感。"**（8/11）——Day 11 主动提出的挫败感。同一天晚上，他 15 分钟零提示一遍跑通了一道嵌套字典题，上午踩的坑晚上就记住了。他在日志里被记了一笔："上午踩的坑，晚上就记住了——这是今天最好的信号。"

4. **"跑通了，但我安静。"**（8/18 前后）——写程序跑通之后，他没有欢呼，只是把输出贴过来。这是一种"我大概知道它对了，但不确定对不对"的谨慎，后来被反复验证是对的——"跑通 ≠ 正确"，很多 bug 是跑通了但结果整个错的。

5. **"我没看废了，换个题吧。"**（8/18）——做到一道"函数当参数"的题（`sorted` + `key=函数`），他判断自己"看废了"，主动止损换题。这个"主动止损"的判断力，是他学编程两周多里练出来的元认知——知道什么时候该死磕、什么时候该换路。

6. **"自己修完告诉你"**（8/9）——第一次自己独立修了一个 bug，从"帮我查 bug"进化到"我自己修完了告诉你"。这是他学习态度的一个里程碑式转折。

---

## ⑤ 和 AI 协作的新细节（与第一周不同）

1. **他反过来纠正 AI**：8/19 给 `--limit 80` 的验证靶子算错了（把 body/title 长度搞混），是泽华用正确代码"对峙"出 AI 的错。之后确立了"先验预期本身对不对"的原则——不再默认 AI 一定对。

2. **他开始主动提出"我要什么"**：以前是拿到题就写，这两周会先追问"你没给我网址""你没给我文件名"。他知道了"一个完整的需求该长什么样"，缺条件会主动要。

3. **他主动要求"重做"和"加练"**：8/10 做完一道题不满意（依赖提示太多），主动要求重做直到独立完成；8/15 主动说"重新写一题检验"（换场景的 `price_report.py`）；8/20 一天 6 个节点，晚上还主动连加两道题。

4. **他主动做"记忆管理"设计**：提出"字符串/列表/字典老搞混"，让 AI 整理一张数据类型速查表；提出"有些代码是固定骨架只换几个值"，让 AI 建了代码模板库，还**自己定了分类标准**（按"查它的目的"分，不按"软硬"分）。

5. **他主动要求考"久远知识点"**：8/20 晚上主动说"帮我复习一个久远一点的知识点，看我还记不记得"，AI 选了两周前的"切片"，结果暴露了"左闭右开"这个盲区。这种"主动测自己"的动作，第一周没有过。

6. **AI 的讲法在变，他也在挑**：卡住时 AI 从"给你完整答案"逐步转向"先问你怎么想的、再给你下一步"。泽华自己总结过"思路对，错在细心"，AI 就顺着这个元认知去引导，而不是空泛鼓励。

7. **环境问题的协作**：8/18 双 Python 冲突时，是 AI 主动排查出两个 Python 版本、教他建虚拟环境、还补了 `.gitignore` 防止把 .venv 推上 GitHub。这种"替他把环境这个非编程但会卡住人的事也兜住"的协作，是第一周没有遇到的新情况。

---

## ⑥ 值得放进文章的画面（只描述"截什么画面"，不用真截）

1. **"两条一模一样的星巴克"**：终端里 `visits_db.py` 跑两次的输出，`[(1, '星巴克', '2026-08-21', '聊了会员体系'), (2, '星巴克', ...)]`——两条内容一样、只有 id 不同，这是"数据库会累积"最直观的证据。

2. **`total["uid"]` vs `total[uid]` 的对比**：两行代码并排，差一对引号，下面配那个"塌成 `{'uid': 146}`"的错误输出。这是"不报错的 bug"的最佳视觉说明。

3. **错题本的"字符毛刺家族"列表**：`ttile`→`title`、`INTERGER`→`INTEGER`、`VAKUES`→`VALUES`、`conn,commit`→`conn.commit`……一串"照抄没核对"的笔误排在一起，很有冲击力。

4. **`TypeError: list indices must be integers or slices, not str`**：这条报错的红色终端截图——它是"用字符串去索引列表"的经典报错，也是泽华两周里最熟悉的一张"脸"。

5. **`55.00000000000001 %`**：这个浮点精度输出的截图，一个"怎么多出这么多小数"的瞬间，配"round 放最后"的口诀。

6. **代码模板库的分类页**：`代码模板库.md` 里"模板（照抄型）"和"速查表（扫一眼）"两个分区，每个模板标注"变化点 + 用在哪"——这是泽华自己定的分类，体现"会学习的人怎么整理"。

7. **README 三句话**：`alert_push.py` 的"首个完整作品"小节——是什么 / 怎么装 / 怎么跑，三句话让陌生人 30 秒上手。

8. **git 提交历史的绿点图**：GitHub 仓库的 commit 时间线，两周 25 次提交、每天都有绿点——"每天往前走一步"的可视化铁证。

9. **`visits.db` 文件出现在目录里**：`ls` 命令后，一堆 .py 文件中间第一次躺着一个 `visits.db`——"程序会留东西在地上"的画面。
